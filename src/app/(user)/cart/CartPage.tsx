import React from "react";

function CartPage() {
  return <div className="mx-auto max-w-3xl p-6 text-sm text-muted-foreground">Future: line items with promo codes, stock holds, upsells, and persistent guest merge on sign-in.</div>;
}

export default CartPage;
